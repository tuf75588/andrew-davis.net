import {graphql} from 'gatsby';

import BlogListing from '../components/BlogListing';

# Hello blog!

<BlogListing />

<!-- we will import some components here soon -->

export const query = graphql`
  {
    allMdx(filter: {fields: {type: {eq: "post"}}}) {
      edges {
        node {
          frontmatter {
            title
          }
        }
      }
    }
  }
`;
