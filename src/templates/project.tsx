import React from 'react';
import Layout from '../components/layout';
function ProjectTemplate({data}: any) {
  return <Layout>this is the PROJECT page template</Layout>;
}

export default ProjectTemplate;
