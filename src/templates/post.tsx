import React from 'react';
import Layout from '../components/layout';
function PostTemplate(props: any) {
  return (
    <Layout>
      this is a post template page! there will be some information here like
      post title, the body of the post, and other specific to post type things
    </Layout>
  );
}

export default PostTemplate;
